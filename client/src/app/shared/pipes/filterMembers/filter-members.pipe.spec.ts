import { FilterMembersPipe } from './filter-members.pipe';

describe('FilterMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMembersPipe();
    expect(pipe).toBeTruthy();
  });
});
