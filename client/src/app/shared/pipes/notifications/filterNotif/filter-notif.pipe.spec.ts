import { FilterNotifPipe } from './filter-notif.pipe';

describe('FilterNotifPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNotifPipe();
    expect(pipe).toBeTruthy();
  });
});
