import { SearchNotifPipe } from './search-notif.pipe';

describe('SearchNotifPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNotifPipe();
    expect(pipe).toBeTruthy();
  });
});
