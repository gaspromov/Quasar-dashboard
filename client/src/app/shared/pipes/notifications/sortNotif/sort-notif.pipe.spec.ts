import { SortNotifPipe } from './sort-notif.pipe';

describe('SortNotifPipe', () => {
  it('create an instance', () => {
    const pipe = new SortNotifPipe();
    expect(pipe).toBeTruthy();
  });
});
