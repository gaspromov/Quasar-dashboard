import { FilterPaymentsPipe } from './filter-payments.pipe';

describe('FilterPaymentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPaymentsPipe();
    expect(pipe).toBeTruthy();
  });
});
