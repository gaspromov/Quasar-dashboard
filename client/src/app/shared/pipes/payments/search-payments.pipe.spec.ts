import { SearchPaymentsPipe } from './search-payments.pipe';

describe('SearchPaymentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPaymentsPipe();
    expect(pipe).toBeTruthy();
  });
});
